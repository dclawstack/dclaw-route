apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-route.fullname" . }}
  labels:
    {{- include "dclaw-route.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dclaw-route.selectorLabels" . | nindent 4 }}
