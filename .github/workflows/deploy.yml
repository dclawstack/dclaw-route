name: Deploy

# Placeholder deploy workflow.
# Triggered on tag push. Real deployment hooks (Helm/ArgoCD/Vercel) wired here later.

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: |
          echo "Deploy step pending — wire Helm/Kubernetes/Vercel actions here."
          echo "Ref: ${{ github.ref }}"
