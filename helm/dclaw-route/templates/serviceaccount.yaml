apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "dclaw-route.serviceAccountName" . }}
  labels:
    {{- include "dclaw-route.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
