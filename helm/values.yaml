replicaCount: 1
image:
  backend:
    repository: dclaw-route-backend
    tag: local-1779987462
    pullPolicy: Never
  frontend:
    repository: dclaw-route-frontend
    tag: local-1779987462
    pullPolicy: Never
service:
  backend:
    type: ClusterIP
    port: 8137
  frontend:
    type: NodePort
    port: 3051
    nodePort: 30067
ingress:
  enabled: false
secret:
  data:
    DATABASE_URL: "postgresql+asyncpg://postgres:postgres@dclaw-shared-postgres:5432/dclaw_route"
    APP_ENV: "production"
    SECRET_KEY: "dev-secret-change-me"
    OLLAMA_URL: "http://ollama:11434"
    OPENROUTER_API_KEY: ""
    NEXT_PUBLIC_API_URL: ""
    ENABLE_DEMO_MODE: "true"
resources:
  requests: { cpu: 10m, memory: 128Mi }
  limits:   { cpu: 300m, memory: 384Mi }
strategy:
  type: Recreate
